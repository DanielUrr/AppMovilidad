name: transporte_app
description: A Flutter transport app with maps, chatbot, and virtual card.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  shared_preferences: ^2.2.2
  http: ^1.1.0
  geolocator: ^11.0.0  # Para obtener la ubicación
  permission_handler: ^11.2.0  # Para manejar permisos

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/rutas.json